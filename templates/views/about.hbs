<div class="container top-gap">
  <div class="row">
    {{#each homepage}}
      {{#if @first}}
        {{#if this.profileImage}}
          <div class="col-sm-6">
            <img class="img-responsive" src="{{this.profileImage.image.url}}" alt="" />
          </div>
        {{/if}}
        <div class="col-sm-6 about-details">
          {{#if this.title}}
            <h2>{{this.title}}</h2>
          {{/if}}
          {{#if this.bio}}
            <p>{{this.bio}}</p>
          {{/if}}
          {{#if this.email}}
            <h2>Contact</h2>
            <ul class="list-inline">
              <li><i class="fa fa-envelope"></i></li>
              <li><a href="mailto:{{this.email}}?Subject=Hello">{{this.email}}</a></li>
            </ul>
          {{/if}}
        </div>
      {{/if}}
    {{/each}}
  </div>
</div>
